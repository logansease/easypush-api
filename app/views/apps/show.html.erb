
<table class="" summary="App info">
  <p>If you are interested in using the Social Scores API in your app, please <%= link_to 'contact me', contact_path %> first. Things are mostly ready to go, but this is still very new, so I'd appreciate you dropping me a line.</p>


   <tr>
		<td class="main">
            <h2><%= @app.app_name %></h2>
         </td>
            <% user = @app.user rescue User.find(@app.user_id)%>
          	 <% if user == current_user%>
          		<td class="delete">
          			<%= button_to "delete", @app, :method => :delete,
          											 :confirm => "Are you sure?",
          											 :title => @app.app_name %>
          		</td>

                 <td class="edit">
       			    <%= button_to "edit", edit_app_path(@app), :method => :get %>
       		        </td>
         	  <% end %>
    </tr>
  </table>
    <table class="well">
      
      <tr>
      <td><%= link_to 'View In App Store', "#{CONSTANTS[  :app_store_url]}#{@app.app_store_id}" %></td>
      </tr>
            <tr>
            <td><b>App Id: </b> <%= @app.app_id.to_i %></td>
            </tr>

     <tr>
            <td><b>Secret Key: </b> <%= @app.app_secret %></td>
     </tr>

      <tr>
                  <td> <%= link_to  'Manage Your Promo Codes', '/promo_codes?app_id=' + @app.id.to_s  %></td>
      </tr>

      <tr>
            <td> <%= link_to  'Push Notifications', '/push_notifications?app_id=' + @app.id.to_s  %></td>
      </tr>

    <tr><td> <%= link_to  'View All Users', '/apps/show_users?app_id=' + @app.id.to_s  %></td></tr>

      <tr> <td><b>Level: </b></td>
      <td><b>Users: </b>   </td>
      <td><b>High Score: </b> </td>
    </tr>

        <% @levels.each do |l|%>
         <tr>
         <td> <%= link_to  l.level_id, '/apps/show_level?level_id=' + l.level_id + '&app_id=' + @app.id.to_s %></td>
         <td> <%= l.score_fb_id %> </td>
         <td> <%= l.score %></td>
         </tr>
              <%  end %>
         

</table>
