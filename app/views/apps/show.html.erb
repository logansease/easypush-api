
<table class="" summary="App info">

   <tr>
		<td class="main">
            <h2><%= @app.app_name %></h2>
         </td>
            <% user = @app.user rescue User.find(@app.user_id)%>
          	 <% if user == current_user%>
          		<td class="delete">
          			<%= button_to "delete", @app, :method => :delete,
          											 :confirm => "Are you sure?",
          											 :title => @app.app_name %>
          		</td>

                 <td class="edit">
       			    <%= button_to "edit", edit_app_path(@app), :method => :get %>
       		        </td>
         	  <% end %>
    </tr>
  </table>
    <table class="well">
      
      <tr>
      <td><%= link_to 'View In App Store', "#{CONSTANTS[  :app_store_url]}#{@app.app_store_id}" %></td>
      </tr>
            <tr>
            <td><b>App Id: </b> <%= @app.app_id.to_i %></td>
            </tr>

     <tr>
            <td><b>Secret Key: </b> <%= @app.app_secret %></td>
     </tr>

      <tr>
            <td> <%= link_to  'Push Notifications', '/push_notifications?app_id=' + @app.id.to_s  %></td>
      </tr>


</table>
